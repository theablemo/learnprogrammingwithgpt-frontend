name: introductory_programming_frontend_teacher
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  localization: ^2.1.0
  flutter_form_builder: ^9.1.0
  http: ^1.1.0
  page_transition: "^2.0.9"
  flutter_spinkit: ^5.2.0
  get: ^4.6.5
  flutter_highlight: ^0.7.0
  clipboard: ^0.1.3
  shimmer: ^3.0.0
  flutter_markdown: ^0.6.17+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
